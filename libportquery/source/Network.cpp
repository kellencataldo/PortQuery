#include <arpa/inet.h>

#include "Network.h"
/*
bool Network::isValidAddress(const std::string& potentialURL) {
    
    struct sockaddr_in socketAddress = { };
    if (1 == inet_pton(AF_INET, potentialURL.c_str(), &(socketAddress.sin_addr)) ) {
        return true;
    }

    return false;
}

*/
